This project is to demonstrate an easy way to send automated eMails using the Outlook application that is wide spread among windows users.

NOTES

Verification message can pop up depending on your security settings
If outlook is not configured, the message will be stored in the outbox until it gets configured
App crashes if outlook is not installed
